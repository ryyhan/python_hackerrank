#playing with lists

fields=["Machine Learning","Data Science","Cyber Security","Data Analytics"]

#print(fields.sort())#not printing anything
#fields.push("w")
#s=fields.sort()
print(fields)


#tuples
"""just a single thing to make sure if the tuple is containing a single element
then make sure a comma is added after it so that python knows its a tuple
otherwise it will mistook it for a variable
"""
tup=(4,)
print(type(tup))